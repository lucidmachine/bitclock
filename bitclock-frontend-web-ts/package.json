{
  "name": "bitclock",
  "version": "1.0.2",
  "description": "A clock made of binary integers to help you practice reading binary.",
  "author": "Eric Slenk <lucid.machine@gmail.com>",
  "license": "MIT",
  "private": true,
  "keywords": [
    "bitclock",
    "binary",
    "clock"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/lucidmachine/bitclock.git"
  },
  "homepage": "https://github.com/lucidmachine/bitclock#readme",
  "bugs": "https://github.com/lucidmachine/bitclock/issues",
  "scripts": {
    "clean": "for d in build test; do if [ -d \"$d\" ]; then rm -r $d; fi; done",
    "reset": "git clean -dfx && git reset --hard && npm i",
    "fix": "prettier \"src/**/*.ts\" --write",
    "build": "npm run clean && webpack",
    "test:unit": "jest --env=jsdom",
    "test": "npm run build && npm run test:unit",
    "watch": "npm run clean && npm run build && webpack --watch & jest --env=jsdom --watch & wait",
    "cov": "npm run build && jest --env=jsdom --coverage && firefox build/coverage/index.html",
    "version": "standard-version",
    "publish": "npm run build && gh-pages -m \"[ci skip] Updates\" -d dist",
    "all": "npm run reset && npm run test",
    "prepare-release": "npm run all version publish"
  },
  "engines": {
    "node": ">=10.15.0"
  },
  "devDependencies": {
    "@babel/core": "^7.10.5",
    "@babel/preset-env": "^7.10.4",
    "@babel/preset-typescript": "^7.10.4",
    "@types/jest": "^24.9.1",
    "@types/node": "^11.15.18",
    "babel-jest": "^26.1.0",
    "bitclock-backend": "^1.1.2",
    "cz-conventional-changelog": "^2.1.0",
    "gh-pages": "^2.2.0",
    "jest": "^26.1.0",
    "jest-date-mock": "^1.0.8",
    "prettier": "^1.19.1",
    "standard-version": "^8.0.2",
    "ts-jest": "^26.1.3",
    "ts-loader": "^5.4.5",
    "typescript": "^3.9.7",
    "webpack": "^4.43.0",
    "webpack-cli": "^3.3.12"
  },
  "config": {
    "commitizen": {
      "path": "cz-conventional-changelog"
    }
  },
  "prettier": {
    "singleQuote": true
  }
}
